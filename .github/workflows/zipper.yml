name: Zipper
on:
  push:
    branches: [ "master" ]
jobs:
  zip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run zipper.sh
        run: /bin/bash ./zipper.sh
